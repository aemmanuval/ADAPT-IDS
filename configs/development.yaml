# Development configuration — smaller dataset for faster iteration

dataset:
  sample_fraction: 0.1
  max_rows: 100000

models:
  lightgbm:
    n_estimators: 50
    max_depth: 6
  random_forest:
    n_estimators: 50
    max_depth: 12

streaming:
  batch_size: 500

logging:
  level: "DEBUG"
